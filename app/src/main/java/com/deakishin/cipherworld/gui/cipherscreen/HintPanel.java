package com.deakishin.cipherworld.gui.cipherscreen;

import android.content.Context;
import android.os.Build;
import android.util.AttributeSet;
import android.view.View;

/** View that displays hint panel for a cipher. */
class HintPanel extends View{



    public HintPanel(Context context) {
        this(context, null, 0);
    }

    public HintPanel(Context context, AttributeSet attrs) {
        this(context, attrs, 0);
    }

    public HintPanel(Context context, AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);


    }


}
